module.exports = {
  apps: [{
    name: '{{ pm2_instance }}',
    script: 'node_modules/next/dist/bin/next',
    args: 'start',
    cwd: '{{ app_dir }}',
    instances: 1,
    exec_mode: 'fork',
    watch: false,
    max_memory_restart: '1G',
    env: {
      NODE_ENV: 'production',
      PORT: {{ pm2_port }},
      DATABASE_URL: 'file:{{ app_dir }}/prisma/dev.db'
    },
    error_file: '{{ logs_path }}/error.log',
    out_file: '{{ logs_path }}/out.log',
    log_file: '{{ logs_path }}/combined.log',
    time: true,
  }]
};
